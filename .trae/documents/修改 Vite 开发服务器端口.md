1. 问题分析：vite.config.ts 文件中服务器配置设置了端口为 80，这是一个特权端口，普通用户无法直接使用，导致启动开发服务器时出现权限错误。

2. 解决方案：将 vite.config.ts 文件中的 port 配置从 80 修改为一个非特权端口（如 3000、5173 等）。

3. 实施步骤：

   * 修改 vite.config.ts 文件，将 server.port 从 80 改为 5173（Vite 默认端口）

   * 保存文件后重新

